# SmartAutomation
Automation

## Vercel test
Deze repo heeft een simpele `index.html` op de root en een extra test in `public/index.html`.
Als je na push “(v2)” ziet, dan is je GitHub → Vercel deployment werkt.
